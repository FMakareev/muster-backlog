# Not a task

A plain markdown file with no frontmatter, sitting inside a tasks directory. Nothing in the
corpus produced one, but a human can drop a README, a `.gitkeep` or an editor backup anywhere.
Intended handling: skip with a diagnostic, do not fail the project scan.
